# Onde o acervo é salvo em disco — o que faz ele sobreviver ao navegador.
#
# Com VAULT_DIR apontando pra uma pasta, cada alteração é gravada nela:
#
#   <pasta>/
#   ├─ acervo.json        boards, referências e preferências
#   └─ imagens/<id>.webp  uma imagem por referência
#
# Limpar os dados do navegador, trocar de perfil ou de navegador não tira nada
# de lá: na abertura seguinte o acervo volta de disco sozinho. Backup é copiar
# a pasta; restaurar é colocá-la de volta.
#
# Em `npm run dev` isso já vem ligado apontando pra ./data — a linha abaixo só
# é necessária pra escolher outro lugar ou pra ligar em produção.
# VAULT_DIR=./data
# VAULT_DIR=/Users/voce/Google Drive/Referências
#
# A pasta não tem senha: quem alcança o app alcança o acervo. Num servidor
# aberto na internet, deixe desligado (VAULT_DIR=off) e use a sincronização.

# Sincronização entre dispositivos — OPCIONAL.
#
# Sem esta variável o app roda 100% local no navegador (IndexedDB), do jeito
# que sempre funcionou. Ao defini-la, aparece a opção de criar conta e o acervo
# passa a sincronizar entre computador e celular.
#
# As tabelas são criadas sozinhas na primeira execução.

# Postgres gerenciado (Supabase, Neon, RDS, Railway…).
# Em provedores com pool de conexões, prefira a URL do pooler (porta 6543 no
# Supabase) — cada instância do app abre até 8 conexões.
# DATABASE_URL=postgres://usuario:senha@host:5432/banco

# Postgres local, sem TLS:
# DATABASE_URL=postgres://app@127.0.0.1:5432/referencias?sslmode=disable

# SQLite em arquivo, para quem hospeda o próprio servidor.
# O diretório precisa ser um volume persistente — num container efêmero o
# arquivo some junto com o container.
# DATABASE_URL=file:./data/referencias.db
